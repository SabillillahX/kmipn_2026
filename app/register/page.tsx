import { RegisterForm } from '@/src/features/auth/components/RegisterForm';

export const metadata = {
  title: 'Register - KMIPN 2026',
  description: 'Create a new KMIPN 2026 account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
